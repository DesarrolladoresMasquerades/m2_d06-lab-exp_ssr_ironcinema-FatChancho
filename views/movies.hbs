<div class="pelis">
    {{#each movies}}
    <div class="object">
        <img src="{{image}}" alt="imagen">
        <p>{{title}}</p>
        <a href="/movies/{{_id}}">See more</a>
    </div>
    {{/each}}
</div>